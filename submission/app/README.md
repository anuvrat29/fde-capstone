# submission/app

Reserved package UI slot. The live interactive runner is the **Taipy** app at
[`submission/app_taipy/`](../app_taipy/README.md).

```bash
python submission/scripts/run_taipy.py
```

This submission does not use Node.js, npm, Vite or React.
