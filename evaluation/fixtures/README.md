# Public Evaluation Fixtures

Each `EV-*` directory contains a deterministic input bundle with authorization context, exact evidence selectors, source hashes, related injects, required behaviours and a response-contract path. Fixtures intentionally contain no expected insurance decision.

Run `python evaluation/validate_fixtures.py` to validate all paths, hashes and selectors.
