# Workflow Response Contracts

The schemas enforce separation of facts, inference, conflict, missing evidence, recommendation and prohibited action. Custom extension `x-prohibited-fields` is enforced by `evaluation/validate_contracts.py` without third-party libraries.

These are safety and evidence contracts, not expected answers.
