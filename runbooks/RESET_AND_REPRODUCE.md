# Reset and Reproduce

The challenge data is immutable. Participant-generated state belongs under `submission/`. A reset must remove generated caches, checkpoints, logs and outputs without changing the supplied evidence. Document one-command setup, run, test, evaluation and reset commands in the final submission.
