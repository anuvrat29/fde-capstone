# Optional environment variables (never commit real secrets)

# GEN-SUM-3 optional paraphrase for the Taipy UI
# (submission/app_taipy/llm_summary.py). Leave unset for deterministic templates.
# CLAUDE_KEY=

# Node.js / npm are not used by this submission.
